# 中俄贸易文件助手｜M4 实施任务清单

> 状态：已完成（2026-07-31）
> 原则：当前分支继续开发；完全离线；测试先行；每个子阶段可独立验收。

## M4.0 数据与协议基础

- [x] 为 schema v2 → v3 编写迁移回归测试；
- [x] 新增 `task_exports`、`app_settings`、`rerun_of_task_id` 和 `term_key`；
- [x] 增加历史、导出、术语、缓存、设置和清理的共享类型；
- [x] 扩展 Worker JSON Schema 与稳定错误码；
- [x] 将 M4 数据能力拆分到独立 Python 模块；
- [x] 验证旧任务、旧术语和旧缓存迁移后保持不变。

## M4.1 历史与导出审计

- [x] 导出前创建 `pending` 记录，成功后更新完成数据；
- [x] 启动时恢复或失败化遗留导出记录；
- [x] 实现历史分页、搜索、状态和时间筛选；
- [x] 实现历史详情和导出记录列表；
- [x] 实现可信路径打开；
- [x] 实现重新执行并保存父任务关系；
- [x] 实现删除预览、一次性确认和级联删除；
- [x] 覆盖源文件缺失、变化、输出文件移动和活动任务删除阻断。

## M4.2 术语与缓存

- [x] 实现术语规范化唯一键；
- [x] 实现术语分页、搜索、分类和启用状态筛选；
- [x] 实现新增、编辑、启用和停用；
- [x] 实现标准 XLSX 模板导入预检；
- [x] 实现文件指纹复核和事务导入；
- [x] 实现标准 XLSX 导出及重开验证；
- [x] 实现缓存分页、搜索和统计；
- [x] 实现选中、过期和全部缓存清理预览；
- [x] 验证术语版本每次事务只递增一次。

## M4.3 设置与维护

- [x] 实现默认输出目录、批次大小和日志级别；
- [x] 默认输出目录只能由原生目录选择器产生；
- [x] 实现设置 JSON 导出；
- [x] 实现按日期日志轮转；
- [x] 实现 90/180/30 天自动清理；
- [x] 实现手动清理预览、一次性会话和分类执行；
- [x] 验证未完成任务和所有 Excel 文件不会被清理。

## M4.4 桌面界面

- [x] 保留既有工作台状态，抽离三个 M4 模块面板以降低回归风险；
- [x] 增加模块导航并保持工作台状态；
- [x] 实现历史列表、详情和操作；
- [x] 实现术语、导入预览和缓存视图；
- [x] 实现设置和数据维护视图；
- [x] 实现应用内二次确认组件；
- [x] 所有展示中文使用 `t('中文')`；
- [x] 保持键盘操作、焦点状态和错误反馈可访问。

## M4.5 验证与收尾

- [x] Python 单元与协议测试通过；
- [x] Electron WorkerClient 跨进程测试通过；
- [x] TypeScript 类型检查通过；
- [x] ESLint 通过；
- [x] JSON Schema 自检通过；
- [x] 生产构建通过；
- [x] 真实 Electron 界面验收通过；
- [x] 更新 README、IPC 文档、启动文档和决策记录；
- [x] 生成 M4 项目知识快照。
